---
import { addBaseUrl } from '../../utils/url';
const { href, ...props } = Astro.props;
const finalHref: string = addBaseUrl(href);
---

<a href={finalHref} {...props} class='clickable-link'>
  <slot />
</a>
